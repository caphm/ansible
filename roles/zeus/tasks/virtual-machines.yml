---
- name: define virtual machines
  virt:
    name: htpc
    command: define
    xml: "{{ lookup('template', 'vm.xml.j2') }}"
    autostart: true

- name: start virtual machines
  virt:
    name: htpc
    state: running
